# (Optional) Secret key for encrypting API keys in cookies (32 bytes = 64 hex chars)
# If not set, a key is auto-generated on startup (sessions reset on server restart)
# For production, set a fixed value:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=
